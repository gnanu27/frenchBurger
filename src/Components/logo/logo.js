import React from 'react';
import logo from '../../assets/images/logo.png'
import './logo.css'

const Logo = (props)=>(
    <div className="Logo">
        <img src={logo} alt="myBurger" />
    </div>
) 

export default Logo;
